{
  "name": "@repo/root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "biome check --write .",
    "check-types": "turbo run check-types",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:codegen": "playwright codegen http://localhost:3000",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "_verify:package": "set -e; REPO_ROOT=$(pwd); cd packages/lexical-ugly-footnotes && pnpm build && TARBALL=$(pnpm pack --silent | tail -n 1) && cd \"$REPO_ROOT\" && TEST_DIR=$(mktemp -d -t test-publish-XXXXXX) && cd \"$TEST_DIR\" && npm init -y >/dev/null && pnpm add \"$REPO_ROOT/packages/lexical-ugly-footnotes/$TARBALL\" && node --input-type=module -e \"import('lexical-ugly-footnotes').then(() => console.log('✓ Package imports successfully')).catch(e => { console.error('✗ Import failed:', e); process.exit(1); })\" && cd \"$REPO_ROOT\" && rm -rf \"$TEST_DIR\" && rm -f \"$REPO_ROOT/packages/lexical-ugly-footnotes/\"*.tgz",
    "verify:package": "node scripts/verify-package.mjs",
    "release:dry-run": "pnpm build && pnpm verify:package && changeset publish --dry-run",
    "_release": "pnpm build && pnpm verify:package && changeset publish",
    "release": "changeset publish",
    "_publish:ci": "pnpm build && pnpm verify:package && npx changeset publish",
    "release:plan": "pnpm build && pnpm verify:package && changeset status --since=main && changeset version --snapshot",
    "pack:inspect:footnotes": "pnpm -C packages/lexical-ugly-footnotes pack:inspect",
    "pack:size:footnotes": "pnpm -C packages/lexical-ugly-footnotes pack:size",
    "publish:ci:oidc": "pnpm build && pnpm verify:package && node ./scripts/publish-oidc-changesets.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@changesets/cli": "^2.27.9",
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0",
    "prettier": "^3.6.0",
    "prettier-plugin-tailwindcss": "^0.7.1",
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@10.19.0",
  "engines": {
    "node": ">=24"
  }
}
